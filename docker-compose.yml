version: '3.1'
services:
  dbpostgresql:
    image: postgres
    container_name: postgresql

    ports:
      - "5432:5432"
    volumes:
      - /.app
    environment:
      POSTGRES_DB: northwind
      POSTGRES_PASSWORD: northwind